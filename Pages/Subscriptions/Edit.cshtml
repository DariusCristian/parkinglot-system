@page
@model ParkingLotSystem.Pages.Subscriptions.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Subscription</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Subscription.ID" />
            <div class="form-group">
                <label asp-for="Subscription.SubscriberID" class="control-label"></label>
                <select asp-for="Subscription.SubscriberID" class="form-control" asp-items="ViewBag.SubscriberID" ></select>
                <span asp-validation-for="Subscription.SubscriberID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Subscription.SubscriptionPlanID" class="control-label"></label>
                <select asp-for="Subscription.SubscriptionPlanID" class="form-control" asp-items="ViewBag.SubscriptionPlanID" ></select>
                <span asp-validation-for="Subscription.SubscriptionPlanID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Subscription.EndDate" class="control-label"></label>
                <input asp-for="Subscription.EndDate" class="form-control" />
                <span asp-validation-for="Subscription.EndDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
